# Generated assets

This branch holds files written by workflows, not source.

`assets/star-history.svg` is regenerated daily by `.github/workflows/star-history.yml`.

It is an orphan branch on purpose: it shares no history with `dev`, so a bot
committing here can never touch the source tree, and `dev`'s branch protection
stays exactly as it is.
